// Reexporta o AuthProvider e o hook useAuth definidos no nível de app.
// Mantém uma única fonte de verdade para autenticação em toda a aplicação.
export { AuthProvider, useAuth } from "@/app/providers/AuthProvider"
